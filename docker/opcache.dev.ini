; Local-dev opcache overrides — loaded after zz-opcache.ini (zzz > zz).
; Makes PHP re-read source files as they change on the bind-mounted host
; tree, so edits show up on the next request with no container restart.
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0
